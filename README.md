# musicX
A fast, flexible backend built with Java Spring Boot, using GraphQL to query and manage tracks, artists, albums, and genres through a single endpoint. Backed by a PostgreSQL database, it delivers clean relationships, efficient querying, and a scalable architecture for modern music applications.
