spring.application.name=musicx
spring.datasource.url=jdbc:postgresql://localhost:5432/musicx_db
spring.datasource.username=pguser
spring.datasource.password=pgpassword
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect
# spring.jpa.hibernate.ddl-auto=update

spring.flyway.enabled=false

# visual interface for writing and executing queries
spring.graphql.graphiql.enabled=true

app.flyway.location=classpath:db/migration
app.jwt.secret_key=3oLlJl1D0tZhNdbQZudjM0SyCddgyYkEB14aPeJQXW8


# Commented Out FlyWay Configurations
# Validates the applied migrations against the available ones.
# spring.flyway.validate-on-migrate=true
# spring.flyway.url=jdbc:postgresql://localhost:5432/musicx_db
# spring.flyway.user=pguser
# spring.flyway.password=pgpassword
# Migration files location
# spring.flyway.locations=classpath:db/migration
# Decouple migrations from the applications lifecycle



# Prevents Flyway from failing when connecting to an existing database that already has tables.
# I don't have to worry about baselining on a greenfield project like this
# spring.flyway.baseline-on-migrate=true